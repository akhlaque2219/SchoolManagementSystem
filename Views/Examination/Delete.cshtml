@model Examination
@{ ViewData["Title"] = "Delete Examination"; }
<div class="mt-4"><h1 class="h3 fw-bold text-danger"><i class="fas fa-trash me-2"></i>Delete Examination</h1></div>
<div class="card border-0 shadow-sm mt-3" style="max-width:500px">
    <div class="card-body p-4">
        <div class="alert alert-danger">Delete <strong>@Model.Title</strong>? This will also delete all its schedules.</div>
        <form asp-action="Delete" method="post">
            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger me-2">Yes, Delete</button>
            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
        </form>
    </div>
</div>
